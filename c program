#include <stdio.h>
int main() {
    double c, d, product;
    printf("Enter two numbers: ");
    scanf("%lf %lf", &c, &d);  
 
    // Calculating product
    product = c * d;

    // %.2lf displays number up to 2 decimal point
    printf("Product = %.2lf", product);
    
    return 0;
}
